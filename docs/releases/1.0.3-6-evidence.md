# BoosterFish 1.0.3 (6) — preuves de build interne

Date du contrôle : 30 juillet 2026  
Commit source : `602ad5709f8e5296fba301ef8c33eea1b627779c`

## Statut

Cette version est destinée à la piste **Test interne** uniquement. Elle ne doit
pas être promue en production avant la validation de la fiche Data Safety, des
déclarations Play, du rapport de pré-lancement, de Play Integrity et des
tests manuels décrits dans la checklist de publication.

## Artefacts officiels

Les deux artefacts ont été générés avec le script obligatoire du dépôt. Le
verrou Gradle a validé la clé Release, le déchiffrement et les **6 365 spots**
officiels avant chaque compilation.

| Artefact | Chemin local | SHA-256 |
|---|---|---|
| Android App Bundle | `build/app/outputs/bundle/release/app-release.aab` | `3d28139e86125fab0a0c3d31cf9fd8e6a9852c32d17a818056d43fe7d5a310ec` |
| APK Release | `build/app/outputs/flutter-apk/app-release.apk` | `4135fe4ac1e94a84ef7938ad11be438f1d8f07cdb5301e02bdf36f0ca66195dd` |

Les artefacts précédemment générés depuis le commit `227ca57` et les hash
`171100…e75c` / `98a2b3…0572` sont **remplacés** par ceux ci-dessus. Ils ne
doivent être ni installés ni téléversés dans Play Console.

## Identité Android vérifiée

- Package : `com.zagorito.spots_app`
- Version : `1.0.3`
- Version code : `6`
- Min SDK : `24`
- Target/compile SDK : `36`
- ABI : `arm64-v8a`, `armeabi-v7a`, `x86_64`
- Certificat d'upload SHA-256 :
  `19368c2c2df86b11c053d0666a1d38a0abe71b9595e3c7fc2da8c8a8d7603f97`
- Clé : RSA 2 048 bits
- Expiration du certificat : 18 novembre 2053

## Contrôles réussis

- `flutter analyze` : aucun problème.
- `flutter test --dart-define-from-file=.env` : 69 réussis, 1 ignoré.
- Android Lint Release : `No issues found`.
- Bundletool Google 1.18.3 : bundle valide.
- APK Signature Scheme v2 : valide.
- ZIP AAB et APK : aucune erreur.
- APK zipalign : valide.
- R8/minification : mapping de 54 177 291 octets.
- Identifiant de mapping Crashlytics :
  `ca9466c78677412993f4266493df9b1e`.
- Scan OSV de 183 paquets Pub : 0 vulnérabilité connue.
- `npm audit --omit=dev` sur les deux backends : 0 vulnérabilité.
- Aucun `.env`, keystore, compte de service, clé privée,
  `google-services.json` ou `key.properties` embarqué.
- `android:allowBackup="false"` et règles d'exclusion complètes.
- `android:usesCleartextTraffic="false"` et ancrages système TLS uniquement.
- Aucune permission caméra, microphone, stockage partagé, notification ou
  localisation en arrière-plan.
- Règles Firestore de signalement compilées et déployées le 30 juillet 2026.
- Index de suppression globale `likes.likerUid` et `users.blockedUid` :
  état distant `READY`.
- Les cinq Cloud Functions de communauté ont été redéployées sans erreur ;
  le nettoyage planifié a répondu HTTP 200 sur la nouvelle révision.

## Point de longévité surveillé

Flutter signale que `cloud_functions` et `firebase_app_check` appliquent encore
le plugin Kotlin historique. Les versions FlutterFire directes sont déjà les
dernières versions compatibles au moment de ce contrôle. Aucune migration
forcée vers le Kotlin intégré d'AGP n'a été effectuée, car elle augmenterait le
risque de régression sans corriger un rejet Play actuel. Ce point doit être
réévalué lors de la prochaine mise à jour FlutterFire ou avant AGP 10.
